version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
      - DB_PATH=./kspdb.db
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: always
